interface:
  display_name: "Proven Cash Yield"
  short_description: "Rank quality-qualified owner-cash yields"
  default_prompt: "Use $proven-cash-yield to calculate or quality-rank Proven Cash Yield."

dependencies:
  tools:
    - type: "mcp"
      value: "massive"
      description: "Massive market and ticker reference data"

policy:
  allow_implicit_invocation: true
