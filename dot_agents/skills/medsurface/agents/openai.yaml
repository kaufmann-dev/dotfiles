interface:
  display_name: "medsurface"
  short_description: "Create meshes and fuse editable NIfTI labelmaps"
  default_prompt: "Use $medsurface to convert or merge these medical images into the requested mesh or NIfTI labelmap."

policy:
  allow_implicit_invocation: true
